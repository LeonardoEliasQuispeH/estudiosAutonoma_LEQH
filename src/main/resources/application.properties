spring.application.name=prueba
#server.port=8081


# URL de la base de datos (asegúrate de que el puerto es correcto, normalmente 3306)
# spring.datasource.url=jdbc:mysql://localhost:3306/lupalegi?useSSL=false&serverTimezone=UTC

# Usuario y contraseña (por defecto en XAMPP)
# spring.datasource.username=root
# spring.datasource.password=

# Driver JDBC
# pring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# === CONEXIÓN A AZURE SQL SERVER ===
spring.datasource.url=jdbc:sqlserver://pruebasestudio.database.windows.net:1433;databaseName=lupalegi;encrypt=true;trustServerCertificate=false;loginTimeout=30;
spring.datasource.username=adminestudios
spring.datasource.password=Estudios123

# Driver para SQL Server
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect